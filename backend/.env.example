# Environment
NODE_ENV=development

# Server Port
PORT=5000

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/aws-pricing-calculator

# JWT Secret (REQUIRED - change this to a strong random string)
# IMPORTANT: Copy this file to .env and set a secure JWT_SECRET
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# AWS Credentials (optional - Pricing API can work without explicit credentials)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
